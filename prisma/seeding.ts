import { quote } from "../logic";

quote.create({
  content: "You're an alcoholic, right?",
  by: "Someone",
});

quote.create({
  content: "You can't beun twice in one day.",
  by: "Someone else",
});
