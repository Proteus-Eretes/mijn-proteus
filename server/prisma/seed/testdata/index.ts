import groups from "./group.json" assert { type: "json" };
import materials from "./material.json" assert { type: "json" };
import members from "./member.json" assert { type: "json" };

export const groupsJson = groups;
export const materialsJson = materials;
export const membersJson = members;
